<div>
    <p>Gestion des taches</p>
    <div class="flex justify-between bg-(--secondary-color) rounded-2xl p-1">
        <a href="{{ path('app_login') }}"   class="text-center {{ currentRoute == 'login' ? ' bg-white' : 'null' }} p-1 rounded-2xl w-full">Connexion</a>
        <a href="{{ path('app_register') }}" class="text-center {{ currentRoute == 'register' ? ' bg-white' : 'null' }} p-1 rounded-2xl w-full">Inscription</a>
    </div>
</div>
